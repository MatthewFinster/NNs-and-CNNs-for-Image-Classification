# NNs-and-CNNs-for-Image-Classification
Exploring neural networks (NNs) and convolutional neural networks (CNNs) for multi-class classification tasks using the MNIST dataset, including hyperparameter tuning.
